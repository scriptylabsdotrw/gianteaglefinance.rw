import { ArrowRight01Icon } from 'hugeicons-react'

/**
 * Shared type for a Hugeicons icon component, used when icons are stored in data.
 * Mirrors the actual component signature (accepts className, size, strokeWidth, color).
 */
export type IconType = typeof ArrowRight01Icon
